# ---- Secrets / local config ----
.env
.env.*
!.env.example
*.pem
*.key
secrets.json
credentials.json
token.json

# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
wheels/
pip-wheel-metadata/

# Virtual environments
.venv/
venv/
env/
ENV/

# Tooling / test caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.pytype/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
coverage.xml
.ipynb_checkpoints/

# ---- Node ----
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/
.npm/
.yarn/
.node_repl_history
*.tsbuildinfo
.eslintcache
.cache/
.parcel-cache/
.next/
out/

# ---- Editors / IDEs ----
.vscode/
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
.\#*
\#*\#
.project
.settings/

# ---- OS cruft ----
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# ---- Logs / scratch ----
*.log
logs/
tmp/
.tmp/

# ---- Daily markdown output ----
# NOTE: the generated daily review output directory is intentionally NOT
# ignored yet, so the generated markdown can be committed/reviewed for now.
# If output should become throwaway later, add the output dir here
# (e.g. `reviews/` or `output/`).

# ---- slack-daily-review runtime ----
# Runtime data now lives outside the repo (~/.slack-daily-review, or
# $SLACK_DAILY_REVIEW_HOME) so a plugin update cannot delete it. These two entries
# cover leftovers from before that move, and any local run that points
# SLACK_DAILY_REVIEW_HOME back at this directory. INBOX.md holds VERBATIM Slack
# content including private DMs -- committing it would put colleagues' messages
# into version control.
state/
INBOX.md
